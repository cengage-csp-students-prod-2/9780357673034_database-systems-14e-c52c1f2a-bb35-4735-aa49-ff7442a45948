SELECT
B.BOOK_NUM,
B.BOOK_TITLE,
A.AU_LNAME,
A.AU_FNAME,
P.PAT_ID,
P.PAT_LNAME,
P.PAT_TYPE
FROM
BOOK B
INNER JOIN
WRITES W ON B.BOOK_NUM = W.BOOK_NUM
INNER JOIN
AUTHOR A ON A.AU_ID = W.AU_ID
INNER JOIN
PATRON P ON B.PAT_ID = P.PAT_ID
ORDER BY
B.BOOK_TITLE;