SELECT
A.AU_ID,
A.AU_FNAME,
A.AU_LNAME
FROM
AUTHOR A
WHERE
A.AU_ID NOT IN (
SELECT W.AU_ID
FROM WRITES W
JOIN BOOK B ON W.BOOK_NUM = B.BOOK_NUM
WHERE B.BOOK_SUBJECT = 'Programming'
)
ORDER BY
A.AU_LNAME;