SELECT
CUSTOMER.CUS_CODE,
CUSTOMER.CUS_BALANCE,
CONVERT(Sum(LINE.LINE_UNITS*LINE.LINE_PRICE), DECIMAL(10,2)) AS 'Total Purchases'
FROM
CUSTOMER,
INVOICE,
LINE
WHERE
INVOICE.INV_NUMBER = LINE.INV_NUMBER AND CUSTOMER.CUS_CODE = INVOICE.CUS_CODE
GROUP BY
INVOICE.CUS_CODE, CUSTOMER.CUS_BALANCE;